---
# All roles pinned to exact tags. This file serves as both the dependency
# declaration and the lockfile. Only roles with existing repos and tags
# are listed — add new roles as they are implemented.

roles:
  # Scaffold (template/reference role)
  - name: scbitworx.scaffold
    src: git+https://github.com/scbitworx/ansible-role-scaffold.git
    version: v0.2.0

  # Core roles
  - name: scbitworx.base
    src: git+https://github.com/scbitworx/ansible-role-base.git
    version: v0.15.1
